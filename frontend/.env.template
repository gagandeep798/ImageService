# Copy to frontend/.env.local and fill in values. Not committed.
#
# LocalStack: set VITE_COGNITO_ENDPOINT + VITE_SAM_LOCAL_API_URL
# Real AWS:   unset both (CloudFront handles routing, Amplify hits real Cognito)

VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# Local dev only
VITE_COGNITO_ENDPOINT=http://localhost:4566
VITE_SAM_LOCAL_API_URL=http://localhost:3000

# Mirrors template.yaml Globals
VITE_MAX_IMAGE_SIZE_BYTES=20971520
VITE_CHUNK_SIZE_BYTES=5242880
