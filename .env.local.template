# Copy this to .env.local and fill in values.
# .env.local is NOT committed — it contains dev credentials.
#
# To override Docker port mappings, create docker/.env.local with e.g.:
#   NGINX_PORT=9090
#   LOCALSTACK_PORT=4567

# LocalStack fake credentials (always "test" for LocalStack)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1

# All AWS service endpoints route through Nginx reverse proxy
DYNAMODB_ENDPOINT_URL=http://localhost:8080/api
S3_ENDPOINT_URL=http://localhost:8080/api
SECRETSMANAGER_ENDPOINT_URL=http://localhost:8080/api
KINESIS_ENDPOINT_URL=http://localhost:8080/api
SQS_ENDPOINT_URL=http://localhost:8080/api

# OpenSearch through Nginx
OPENSEARCH_ENDPOINT=http://localhost:8080/opensearch

# Full bucket names for local (account 000000000000, region us-east-1)
ORIGINALS_BUCKET=image-service-originals-000000000000-us-east-1
THUMBNAILS_BUCKET=image-service-thumbnails-000000000000-us-east-1
QUARANTINE_BUCKET=image-service-quarantine-000000000000-us-east-1
LOGS_BUCKET=image-service-logs-000000000000-us-east-1

# Secrets are loaded from .env.secrets (generated by scripts/fetch_secrets.sh)
# PII_PEPPER=<fetched from Secrets Manager>
